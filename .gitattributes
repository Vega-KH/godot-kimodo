* text=auto
*.gd text eol=lf
*.tscn text eol=lf
*.godot text eol=lf
# Recorded fixtures are byte-for-byte provenance artifacts; never normalize.
*.gltf -text -diff
*.json -text -diff
*.res -text -diff
